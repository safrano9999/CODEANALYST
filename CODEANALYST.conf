# CODEANALYST — Config
# key=value format

# Web Server
host=127.0.0.1
port=11000

# Project Scan Paths
# One path per line under [paths]. Relative paths are relative to this config file.
# Default: ../ = all sibling folders
[paths]
../

# Examples (uncomment to add):
# /home/rafael/github/CLAWBRIDGE
# /srv/shared/projekte
